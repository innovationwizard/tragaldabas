# Nixpacks configuration for Railway
# Railway will auto-detect Python and use this config

[phases.setup]
nixPkgs = ["python312", "pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements-full.txt"
]

[start]
cmd = "python worker.py"

