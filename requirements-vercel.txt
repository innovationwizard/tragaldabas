# Minimal requirements for Vercel deployment
# Only include essential packages for the API

# Web framework (required)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Core dependencies
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Supabase Auth (required for API)
supabase>=2.0.0

# Vercel adapter for FastAPI
mangum>=0.17.0

# Configuration
python-dotenv>=1.0.0

# Note: Heavy dependencies like pandas, numpy, LLM libraries are loaded lazily
# when needed, not at import time, to reduce serverless function size

