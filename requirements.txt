# Minimal requirements for Vercel deployment
# Includes essential packages for API and pipeline processing

# Web framework (required)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Core dependencies
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.0.0  # Required for EmailStr validation

# Supabase Auth (required for API)
supabase>=2.0.0

# Configuration
python-dotenv>=1.0.0

# Data processing (required for pipeline stages)
pandas>=2.2.0
numpy>=1.26.0
openpyxl>=3.1.2  # For Excel files
python-docx>=1.1.0  # For Word files

# Utilities (required for file parsing)
chardet>=5.2.0  # Encoding detection
rapidfuzz>=3.5.2  # Fuzzy matching
python-dateutil>=2.8.2  # Date parsing

# LLM providers (required for pipeline processing)
# At least one must be configured via environment variables
anthropic>=0.18.0
openai>=1.12.0
google-generativeai>=0.3.0

# Database (required for pipeline)
psycopg2-binary>=2.9.9

# Output (required for pipeline)
python-pptx>=0.6.23  # PowerPoint output

# Note: These dependencies are needed because pipeline modules are now deployed
# They are imported lazily when pipeline runs, not at API startup

