# Minimal requirements for Vercel API deployment
# Heavy pipeline processing is handled by the worker service (Railway/Render)

# Web framework (required)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Core dependencies
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.0.0  # Required for EmailStr validation

# Supabase Auth (required for API)
supabase>=2.0.0

# HTTP client for calling Edge Functions
httpx>=0.24.0

# Configuration
python-dotenv>=1.0.0

# Note: Pipeline processing dependencies (pandas, numpy, LLM libraries, etc.)
# are NOT included here. They are in requirements-full.txt for the worker service.
# The Vercel API only handles:
# - Authentication
# - Job creation/status queries
# - Frontend serving
# - Delegating processing to the worker service
